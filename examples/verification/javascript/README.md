# JavaScript Verification Boundary

The JavaScript adapter is an official TypeScript Compiler API extension point.
Until that integration is available, `vz validate` verifies its deterministic
unavailable-frontend diagnostic instead of fabricating semantic output.
