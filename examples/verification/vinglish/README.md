# Vinglish Verification Sources

Vinglish validation consumes the stable semantic-export JSON fixtures in
`../../../tests/fixtures/`. It deliberately does not require a compiler binary
and therefore preserves the compiler-to-Zero transport boundary.
