# TypeScript Verification Boundary

The TypeScript adapter is registered but its compiler API integration is not
bundled. Validation records and tests this explicit boundary without treating
TypeScript as an analyzed frontend.
