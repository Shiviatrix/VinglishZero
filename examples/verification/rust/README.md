# Rust Verification Boundary

The `.rs` extension is registered, but a stable official Rust frontend is not
yet bundled. `vz validate` verifies its deterministic unavailable-frontend
diagnostic; it does not report Rust as an analyzed language.
