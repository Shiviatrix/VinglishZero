# Python Verification Sources

Canonical Python programs are under `../../python/`. They cover all current
core patterns through individual examples plus `collection_patterns.py` and
`compositions.py`.
