# Generated Visuals

The showcase PDF renders its architecture, evidence, and constraint diagrams as
TikZ vector graphics in `latex/showcase.tex`. Keeping the diagram source in the
LaTeX document makes the published PDF self-contained and preserves vector
quality without a raster export step.
